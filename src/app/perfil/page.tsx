"use client"; import { AppShell } from "@/components/AppShell"; import { Protected } from "@/components/Protected"; import { Badge,Card } from "@/components/ui"; import { useAuth } from "@/contexts/AuthContext";
export default function Profile(){return <Protected role="employee"><ProfileInner/></Protected>}function ProfileInner(){const {profile}=useAuth();return <AppShell title="Meu perfil"><Card className="profile-card"><div className="profile-avatar">{profile?.name.slice(0,2).toUpperCase()}</div><div><h2>{profile?.name}</h2><p>{profile?.email}</p><Badge tone={profile?.active?"success":"danger"}>{profile?.active?"Conta ativa":"Conta inativa"}</Badge></div></Card></AppShell>}
